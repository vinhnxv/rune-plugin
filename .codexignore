# .codexignore — Prevent Codex from reading sensitive files
# Required for codex exec --full-auto (security prerequisite)

# Secrets and credentials
*.pem
*.key
*.p12
.env*
*credentials*
*secrets*
*token*

# Internal configuration
.claude/
.git/

# OS files
.DS_Store
Thumbs.db
