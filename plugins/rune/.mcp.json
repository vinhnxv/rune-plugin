{
  "mcpServers": {
    "echo-search": {
      "type": "stdio",
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/echo-search/server.py"],
      "env": {
        "ECHO_DIR": "${CLAUDE_PROJECT_DIR}/.claude/echoes",
        "DB_PATH": "${CLAUDE_PROJECT_DIR}/.claude/echoes/.search-index.db"
      }
    }
  }
}
